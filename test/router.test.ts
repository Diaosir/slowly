import { App, Router} from '../lib'

// describe("router test",() => {
//   test('parseRoute', async (done) => {
//     // const result = Router.parseRoute('init <dir> [...otherDirs] [-q | --quiet] <-a | --action>')
//     expect(1+1).toBe(2)
//   })
// })
